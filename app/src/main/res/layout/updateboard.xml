<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp">

    <!-- 게시글 수정 취소버튼 -->
    <Button
        android:id="@+id/buttonCanlcel"
        android:layout_width="58dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="-258dp"
        android:layout_toLeftOf="@id/textview_title_write"
        android:background="@drawable/button_shape2"
        android:text="취소" />

    <!-- 게시글 글쓰기 text -->
    <TextView
        android:id="@+id/textview_title_write"
        android:layout_width="135dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="글 쓰기"
        android:textSize="40sp" />

    <!-- 게시글 수정 text -->
    <TextView
        android:id="@+id/textview"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:ems="10"
        android:layout_toRightOf="@id/textview_title_write"
        android:text="수정"
        android:layout_marginTop="20dp"
        android:textSize="20sp" />

    <!-- 게시글 글쓰기 수정 완료 버튼 -->
    <Button
        android:id="@+id/buttonSubmit"
        android:layout_width="58dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="129dp"
        android:layout_marginBottom="20dp"
        android:layout_toRightOf="@id/textview_title_write"
        android:background="@drawable/button_shape1"
        android:text="수정" />

    <!-- 게시글 글쓰기 수정 할 제목란 -->
    <EditText
        android:id="@+id/EditText_title"
        android:layout_width="380dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/textview_title_write"
        android:layout_marginBottom="10dp"
        android:ems="10"
        android:hint="제목"
        android:textSize="20sp" />

    <!-- 게시글 글쓰기 수정 할 내용란 -->
    <EditText
        android:id="@+id/EditText_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/textview_title_write"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="10dp"
        android:ems="10"
        android:hint="내용"
        android:textSize="15sp" />


</RelativeLayout>
